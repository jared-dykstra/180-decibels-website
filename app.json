{
  "buildpacks": [
    {
      "url": "heroku/heroku-buildpack-nodejs"
    }
  ],
  "env": {
  },
  "environments": {
    "test": {
      "env": {
        "CI": "true"
      },
      "scripts": {
        "test-setup": "yarn run build",
        "test": "yarn run ci"
      }
    }
  }
}
